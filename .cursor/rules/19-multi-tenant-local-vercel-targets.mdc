---
description: Onboarding tenants locally/on Vercel with branded deployments (prefixed env, tooling scripts, `site_theme`).
globs:
  - scripts/run-dev-with-target.mjs
  - scripts/pull-vercel-env.mjs
  - scripts/vercel-targets.json
  - package.json
  - .env.example
  - .gitignore
alwaysApply: false
---

# Multi-tenant workflow (local + Vercel — goldenenglish)

## Mandatory briefing inputs

**Operators/agents:** for “provision new tenant”, complete **`docs/runbooks/add-new-tenant.md`** tables (slug, Vercel project IDs, `SiteThemeKind`, env scaffolding, …) before guessing implementation completeness.

## When to read this rule

Adding another **deployment target** (distinct Vercel project/Supabase/brand) leveraging the repo’s single-codebase/multi-target playbook.

## Checklist — operations (without new DB-backed brand entries)

1. **`public/images/<slug>/`** — landing asset folders (`inicio/`, `historia/`, etc.); add `.gitkeep` when empty scaffolding must exist in git.
2. **`.env.local.<slug>`** tracked via `.gitignore` alongside `.env.local.golden` conventions.
3. **`scripts/run-dev-with-target.mjs`** — extend `TARGETS`, CLI aliases `--<slug>` / `--target=<slug>`, `GE_DEV_TARGET`, optional prompts.
4. **`SITE_BRAND_THEME_SLUG`** — when pinning brand themes via slug serverside, whitelist inside `BRAND_THEME_TARGETS` in that script plus `.env.example` notes.
5. **`scripts/pull-vercel-env.mjs`** — extend `OUT_FILES.<slug>` → `.env.local.<slug>`.
6. **`scripts/vercel-targets.json`** — `orgId`, `projectId`, `projectName`; optional `envPullEnvironment`. Empty placeholders block `npm run deploy:vercel` until curated (or defer to **`scripts/vercel-targets.local.json`**, ignored).
7. **`package.json`** — optional `dev:<slug>`, `pull:env:<slug>` ergonomics adopted by contributors.

## Checklist — brand-specific templates (`SiteThemeKind`)

When exposing bespoke CMS/marketing scaffolding (Mozarthitos / EspacioZenit pattern):

1. **Migration:** `ALTER TYPE public.site_theme_kind ADD VALUE '<slug>'` (`093_*`-style idempotency).
2. **Seed:** `site_themes` rows aligning `slug`/`template_kind` (`094_*`, `100_*`, …).
3. **TypeScript catalogs:** [`src/types/theming.ts`](src/types/theming.ts), `SITE_THEME_KINDS`, CMS catalogs (`landing*Catalog.ts`), [`resolveLandingMedia.ts`](src/lib/cms/resolveLandingMedia.ts), `isSiteThemeKind` tests.
4. **Landing experience:** parameterized/dedicated CSS & components referencing [`marketingLandingKinds`](src/lib/theme/marketingLandingKinds.ts) chrome when full-bleed treatments apply.
5. **Dictionaries:** `landing.<brandRoot>.*` + admin selectors `kindPicker.options.<slug>`.

## References

- Runbook prerequisites: [`docs/runbooks/add-new-tenant.md`](../../docs/runbooks/add-new-tenant.md).
- Example tenants: `golden`, `mozarthitos`, `espaciozenit`.
- Deploy playbook: [`scripts/README.md`](../scripts/README.md#deploy-a-vercel-multi-tenant).
- Promote-all shortcut: `npm run deploy:vercel:all:prod` ≡ `node scripts/deploy-vercel.mjs --all --prod`.
- Sensitive Vercel env documentation: `.env.example` plus official docs.
