/** Labels for {@link AdminGlobalContentMaterialsPanel} (academic global content + blog). */
export interface ContentMaterialsPanelLabels {
  draftMaterialsTitle: string;
  draftMaterialsLead: string;
  materialLabelPlaceholder: string;
  embedUrlPlaceholder: string;
  builderAddEmbed: string;
  builderFileLabel: string;
  builderFileHint: string;
  noMaterialsDraft: string;
  dragMaterial: string;
  dragHandle: string;
  embedKind: string;
  fileKind: string;
  moveUp: string;
  moveDown: string;
  remove: string;
}
