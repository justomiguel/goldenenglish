/**
 * Stub for Next.js' `server-only` package used by Vitest. The real package throws when imported
 * from a client bundle; in tests we just need a no-op so server modules load under jsdom.
 */
export {};
