/**
 * Name of the request header set in `updateSession` from the URL prefix
 * (`/{locale}/...`) so `app/layout.tsx` can set `<html lang>`.
 */
export const GE_REQUEST_LOCALE_HEADER = "x-ge-locale";
