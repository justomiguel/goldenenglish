/** Thrown by `assertAdmin` — compare with `e instanceof Error && e.message` (not user-facing copy). */
export const ADMIN_SESSION_UNAUTHORIZED = "ADMIN_SESSION_UNAUTHORIZED";
export const ADMIN_SESSION_FORBIDDEN = "ADMIN_SESSION_FORBIDDEN";
