# Commit leftover e2e/section navigation hardening

**Intent:** Commit remaining WIP: section create navigation without `refresh` race, section-load warn logs, payment review button a11y (title-only), and e2e timeout/locator hardening.

**Done when:** Changes committed on `chore/spec-driven-harness-rules`.

**Out of scope:** New features.
